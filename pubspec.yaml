name: poc_detect_object_tflite
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_mlkit_object_detection: ^0.13.0
  image_picker: ^1.1.1
  get: ^4.6.6
  image_cropper: ^6.0.0
  image: ^4.2.0
  #  image: ^3.1.3
  path_provider: ^2.1.3
  freewill_fx_widgets: ^0.0.3
  android_id: ^0.3.6
  package_info_plus: ^8.0.0
  uuid: ^4.4.0
  palette_generator: ^0.3.3+3
  google_mlkit_image_labeling: ^0.12.0
  firebase_core: ^3.1.0
  tflite_v2: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/model/
    - assets/test_model/
    - assets/breathalyzer_model/
